"use client"
import { useTranslation } from "../context/TranslationContext"
import { useState } from "react"

export default function LanguageControls() {
  // keep the hooks if you’ll reuse later, or remove them too
  return null
}
